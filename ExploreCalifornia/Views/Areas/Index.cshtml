@model IEnumerable<ExploreCalifornia.Models.Area>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Entry)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Test)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Entry.EntryID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Test)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AreaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AreaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AreaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
